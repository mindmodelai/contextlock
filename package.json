{
  "name": "contextlock",
  "private": true,
  "version": "0.0.1",
  "description": "Trusted Content Verification plugin system for AI coding tools",
  "workspaces": [
    "packages/core",
    "packages/cli-publisher",
    "packages/cli-user",
    "packages/adapter-claude-code",
    "packages/adapter-openclaw"
  ],
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "build": "tsc --build"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.1",
    "fast-check": "^4.1.1"
  },
  "dependencies": {
    "@noble/ed25519": "^2.3.0",
    "@noble/hashes": "^1.7.2",
    "minimatch": "^10.0.1"
  }
}
